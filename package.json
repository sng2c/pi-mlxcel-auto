{
  "name": "pi-mlxcel-auto",
  "version": "0.4.3",
  "description": "Auto-discover running mlxcel-server (OpenAI-compatible) on localhost, read each model's real context window from the local MLX store config.json, and register it with pi. No manual ctx-size entry.",
  "license": "MIT",
  "author": "sng2c",
  "keywords": ["pi-package", "mlx", "mlxcel", "local-models", "model-discovery"],
  "repository": {
    "type": "git",
    "url": "https://github.com/sng2c/pi-mlxcel-auto"
  },
  "pi": {
    "extensions": ["./extensions"]
  },
  "dependencies": {
    "@huggingface/jinja": "^0.5.9"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  }
}